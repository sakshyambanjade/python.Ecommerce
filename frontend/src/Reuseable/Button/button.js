import { Button, Space } from 'antd';
import { ShoppingCartOutlined } from '@ant-design/icons';

const Buttons = () => (
  <Space wrap>
    <Button type="primary"><ShoppingCartOutlined />
    </Button>
  </Space>
);
export default Buttons;